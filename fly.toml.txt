app = "tg-voice-translator"
primary_region = "fra"

[build]
  builder = "heroku/buildpacks:20"

[env]
  PORT = "8080"

# ЯВНО указываем команду запуска
[processes]
web = "python app.py"  # ← ВАЖНО!

[[services]]
  internal_port = 8080
  protocol = "tcp"
  processes = ["web"]  # ← ссылаемся на процесс выше
  
  [[services.ports]]
    handlers = ["http"]
    port = 80
  
  [[services.ports]]
    handlers = ["tls", "http"]
    port = 443

