app = "tg-voice-translator"
primary_region = "fra"

[build]
  builder = "heroku/buildpacks:20"

[env]
  PORT = "8080"

[processes]
web = "python bot.py"  # ← ЯВНО УКАЗЫВАЕМ КОМАНДУ

[[services]]
  internal_port = 8080
  protocol = "tcp"
  processes = ["web"]  # ← ССЫЛАЕМСЯ НА ПРОЦЕСС
  
  [[services.ports]]
    handlers = ["http"]
    port = 80
